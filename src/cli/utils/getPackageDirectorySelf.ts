

export function getPackageDirectorySelfSync() {
    
}

/*

Quick explainer on why can't just use getPackageDirectory from @andyrmitchell/file-io. 

That package uses getInvokedScriptDirectory to figure out where it is.


*/